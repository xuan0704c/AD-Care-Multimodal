====================================================
Update Log - 2026-06-08
====================================================

[更新编号]: UPDATE-2026-0608-003
[更新日期]: 2026-06-08
[操作分支]: branch01
[操作人员]: AI Assistant

[更新内容]:
- 修改文件: js/assessment/preparationLayer.js
  - 修改说明: 
    1. checkMicrophone() 中保留音频流到 window.previewAudioStream，不再在检测后停止
    2. resetChecks() 中增加清理 window.previewAudioStream 的逻辑
  - 影响范围: 准备阶段设备检测、音频流生命周期

- 修改文件: js/utils/mediaRecorder.js
  - 修改说明: 
    1. requestPermissions() 中优先复用 window.previewStream 和 window.previewAudioStream
    2. 仅当全局流不存在或已结束时，才调用 getUserMedia() 重新请求权限
    3. stopRecording() 中停止轨道后清理全局流引用
  - 影响范围: 评估阶段媒体录制、权限请求流程

[更新原因]:
避免用户在准备阶段已授予摄像头和麦克风权限后，进入评估阶段再次弹出权限请求，
提升用户体验并减少不必要的系统调用。

[测试结果]:
- [x] 功能测试通过
- [x] 流复用逻辑检查通过
- [x] 代码审查通过

[备注]:
视频流在准备阶段已通过 window.previewStream 保留，音频流现通过 window.previewAudioStream 保留。
MediaRecorder 在 requestPermissions() 中会检查流是否活跃（readyState === 'live'），确保安全复用。
录制停止后会统一清理全局引用，避免内存泄漏。

====================================================
