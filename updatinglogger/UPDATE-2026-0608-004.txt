====================================================
Update Log - 2026-06-08
====================================================

[更新编号]: UPDATE-2026-0608-004
[更新日期]: 2026-06-08
[操作分支]: branch01
[操作人员]: AI Assistant

[更新内容]:
- 修改文件: index.html
  - 修改说明: 
    1. init() 中 localStorage.getItem() 改为 sessionStorage.getItem()
    2. startAssessment() 中 localStorage.setItem() 改为 sessionStorage.setItem()
    3. deleteAndExit() 中 localStorage.removeItem() 改为 sessionStorage.removeItem()
  - 影响范围: 准备阶段状态持久化机制

[更新原因]:
使程序在浏览器中被强制关闭后，再次进入需要重新经过准备阶段。
sessionStorage 在页面刷新时保留，但在标签页或浏览器关闭后自动清除，
更符合用户的使用场景和安全需求。

[测试结果]:
- [x] 功能测试通过
- [x] 浏览器兼容性检查通过（sessionStorage 为现代浏览器标准 API）
- [x] 代码审查通过

[备注]:
- 页面正常刷新：sessionStorage 保留，仍跳过加载动画和准备阶段
- 关闭标签页/浏览器后重新进入：sessionStorage 被清除，重新经过准备阶段
- 用户点击"删除数据并退出"也会清除该标记

====================================================
