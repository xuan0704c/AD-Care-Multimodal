====================================================
Update Log 2026-06-10-007
====================================================

1. 身份确认界面患者列表优化
   - 按注册时间（regTime）倒序排序，最新录入的患者显示在最前面
   - 默认仅显示前 5 个患者，超出部分自动折叠隐藏
   - 新增"展开更多 (N)"按钮，点击后展开显示剩余患者
   - 展开后按钮变为"收起"，支持再次折叠

2. 修改 index.html renderIdentityList()
   - 增加按 regTime 排序逻辑
   - 增加 SHOW_COUNT = 5 限制
   - 为超出的项添加 identity-patient-hidden 类
   - 动态生成展开/收起按钮并绑定点击事件

3. 修改 css/style.css
   - 新增 .identity-patient-hidden 默认 display: none
   - 新增 .identity-toggle-btn 展开按钮样式（居中、圆角、hover 变色）
====================================================
